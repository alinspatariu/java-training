package Project1;

import java.util.ArrayList;

public interface Clients {

    void addclient(Client client);
    void removeclient(Client client);
    void notifyClients(Car car);
    void updatecar(Car car);
}
